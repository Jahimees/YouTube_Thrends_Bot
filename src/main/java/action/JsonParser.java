package action;

import com.google.gson.Gson;

import java.util.List;

public class JsonParser {

    public List parse(StringBuilder builder) {
        Gson g = new Gson();
        //g.fromJson(builder.toString(), youtubeclass);
        return null;
    }
}
